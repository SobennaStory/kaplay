language: node_js

node_js:
  - "24"

cache:
  directories:
    - ~/.pnpm-store

before_install:
  - npm install -g pnpm

install:
  - pnpm install

script:
  # TypeScript validation
  - pnpm run check
  # Formatting validation
  - pnpm run fmt
