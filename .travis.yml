language: node_js

node_js:
  - "24"

cache:
  directories:
    - ~/.pnpm-store

before_install:
  - npm install -g pnpm@9

install: false

script:
  - pnpm install --frozen-lockfile
  - pnpm run check
  - pnpm run fmt
