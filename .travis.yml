language: node_js

node_js:
  - "24"

cache:
  directories:
    - ~/.pnpm-store

before_install:
  # Install pnpm >= 9.1
  - npm install -g pnpm@9

install:
  - pnpm install --frozen-lockfile

script:
  # TypeScript validation
  - pnpm run check
  # Formatting validation
  - pnpm run fmt
