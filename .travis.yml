language: node_js

node_js:
  - "24"

cache:
  directories:
    - ~/.pnpm-store

before_install:
  # Install pnpm
  - npm install -g pnpm@9

install:
  # IMPORTANT: install workspace deps, not npm
  - pnpm install -r --frozen-lockfile

script:
  - pnpm run check
  - pnpm run fmt
